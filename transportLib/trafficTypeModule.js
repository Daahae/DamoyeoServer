
module.exports.trainType = function(trafficType){// 교통수단의 종류 선택 모듈
    if(trafficType == 1)
      return true;
    else
      return false;
}
module.exports.busType = function(trafficType){// 교통수단의 종류 선택 모듈
    if(trafficType == 2)
      return true;
    else
      return false;
}
module.exports.walkType = function(trafficType){// 교통수단의 종류 선택 모듈
    if(trafficType == 3)
      return true;
    else
      return false;
}
